contract C {
    function f(uint256[] calldata x, uint256 s, uint256 e) external returns (uint256) {
        (x[s:e]).length;
    }
    function f() external returns (uint256) {
    }
    function f_s_only() external returns (uint256) {
    }
    function f_e_only() external returns (uint256) {
    }
    function g() external returns (uint256) {
    }
    function gg() external returns (uint256) {
    }
    function gg_s_only() external returns (uint256) {
    }
    function gg_e_only( uint256 idx) external returns (uint256) {
    }
}
